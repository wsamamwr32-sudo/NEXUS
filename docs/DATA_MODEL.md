# Data Model\n\nPostgreSQL-first relational model with explicit migrations.\n\nGroups: identity; execution; crm; support; knowledge; planning; automation; reliability; ai; integration.\n\nRules: schema changes require migrations; tenant data receives database-level isolation where appropriate; object storage holds file bytes while relational rows hold metadata and authorization context; secrets never enter source control.\n