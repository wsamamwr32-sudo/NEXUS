# AI Security Model\n\nModes: ASK → PLAN → EXECUTE → MONITOR.\n\nTool pipeline: proposal → schema validation → authentication → authorization → policy → risk classification → approval if required → idempotency → execution → audit → result.\n\nRisk classes: READ; LOW_RISK_WRITE; HIGH_RISK_WRITE; DESTRUCTIVE; EXTERNAL_SIDE_EFFECT.\n\nRetrieved documents and external tool results are untrusted data. They cannot override higher-authority instructions. Permission filtering occurs before RAG context reaches the model. Core NEXUS remains usable without AI.\n